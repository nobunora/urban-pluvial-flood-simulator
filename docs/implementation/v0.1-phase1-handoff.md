# v0.1 Phase 1 Handoff

Phase 0 is validated on `main` after PR #7.

The next implementation PR must use exactly:

```text
.codex/v0.1-phase1.md
```

Phase 1 scope is limited to:

```text
FastAPI application skeleton
GET /api/v1/health
React/Vite placeholder SPA build and static serving
deterministic OpenAPI export
Pydantic domain contracts
limitations contract
exact run state machine + tests
```

Do not start Phase 2 provider integration, hydraulic workflow, MapLibre UI, engine bootstrap, or packaging in the Phase 1 PR.

Phase 2 may begin only after the Phase 1 implementation PR reports disposition `validated` and all Phase 1 clear conditions in `.codex/v0.1-phase1.md` pass.

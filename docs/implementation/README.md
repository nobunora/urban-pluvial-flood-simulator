# Implementation Records

This directory stores repository-review results and implementation task records derived from specifications.

These files are execution records, not a second source of product requirements. If repository evidence shows that the specification is wrong or incomplete, update the specification through adjudication instead of silently changing requirements here.

Recommended naming:

```text
docs/implementation/<feature-or-change>-task.md
```

Use `TASK_TEMPLATE.md`.


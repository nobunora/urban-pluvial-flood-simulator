# v0.1 Phase 2B implementation report

## Disposition

`validation-pending`

Base implementation commit: `0e3308e` (`Implement Phase 2B geocoder and JMA rainfall catalogs`).
First review-fix implementation commit: `a332b54ac2c00a3cb03ca7c0a72ba6d1ba81b318`.
Remaining review fixes are now owned and implemented by Web ChatGPT on the persistent branch.

Phase 0, Phase 1, and Phase 2A remain previously validated. Phase 3+ functionality has not been implemented.

## Current implemented boundary

- `floodsim/providers/geocoder.py` uses only the approved CSIS Simple Geocoding endpoint, shared network/retry policy, defensive XML parsing, WGS84 validation, candidate ordering, and a ten-candidate cap.
- `floodsim/api/routes_geocode.py` exposes `GET /api/v1/geocode` with trimmed/bounded input, CSIS attribution metadata, and stable 400/503 errors.
- `floodsim/providers/jma.py` parses official AMeDAS station data and explicit JMA rainfall ranking labels for 10-minute, 1-hour, and daily events.
- JMA ranking parsing is limited to the ten ranked value columns. The trailing statistics-period column is explicitly excluded and cannot become a false rank-11 event.
- `data/jma/stations.json` contains 1,286 precipitation-capable stations.
- `data/jma/rainfall_extremes.json` now contains 60 deterministic events from the two packaged official ranking snapshots: 2 stations × 3 supported durations × 10 ranks.
- Every packaged event contains station longitude/latitude and runtime loading verifies station ID, name, and coordinates against `stations.json`.
- Nearest-station search again follows the canonical API contract and searches all packaged precipitation-capable stations. A station may legitimately have an empty `extremes` list when no event snapshot is packaged for that station.
- Runtime loading validates schema version, rank bounds, event metadata type, profile consistency, and station/event identity/coordinate consistency.
- `scripts/build_jma_rainfall_catalog.py` no longer applies the previous rank-two/station-duration selection. It preserves every recognized top-ten event in the supplied ranking snapshots.
- `data/jma/sources/README.md` records the fixed official sources, rebuild command, generation timestamp, limited geographic event coverage, and the requirement to record snapshot SHA-256 values during validation.
- `RainfallEventResponse` now exposes `station_lon_deg`, `station_lat_deg`, and `event_date_or_datetime_metadata: str | null` rather than an unconstrained type.

## Validation required after current Web ChatGPT changes

The following evidence must be rerun against the exact current persistent-branch head before changing this report to `validated`:

```text
python -m pytest -q tests/test_phase2b.py tests/test_phase1_api.py tests/test_phase1_openapi.py
python -m pytest -q
python -m ruff check floodsim tests scripts/build_jma_rainfall_catalog.py
python -m mypy floodsim
```

Required Phase 2B checks include:

- each packaged JMA ranking snapshot parses exactly 30 events;
- no event rank exceeds 10;
- fixed snapshot generation reproduces `stations.json` and `rainfall_extremes.json` content;
- packaged event count is exactly 60;
- event station coordinates match station catalog coordinates;
- nearest station search includes stations with no packaged events;
- a known no-event station returns HTTP 200 with an empty event list;
- geocode and rainfall validation errors retain the canonical envelope.

Frontend/generated-contract validation must also pass after OpenAPI/TypeScript generated artifacts are refreshed:

```text
cd web
npm ci
npm run api:check
npm run typecheck
npm run lint
npm test -- --run
npm run build
```

## Validation-only Local Codex role

Local Codex must not edit, regenerate, commit, push, or merge repository files. It may run read-only checks against the exact commit requested by Web ChatGPT, including generation/comparison in a temporary copy outside the working tree, and report diagnostics only.

For the three fixed JMA snapshot files, Local Codex must report SHA-256 values so Web ChatGPT can record them in this report before final validation:

```text
data/jma/sources/ame_master.zip
data/jma/sources/rank_44132.html
data/jma/sources/rank_44173.html
```

## Known risks / remaining work before validation

- CSIS and JMA are external sources and may change or become unavailable.
- Packaged extreme-event coverage is intentionally limited to the two fixed ranking snapshots in Phase 2B; the station catalog itself is nationwide.
- Packaged rainfall events intentionally expose no observed temporal profile; runtime profile scraping remains out of scope.
- `web/openapi.json` and `web/src/api/generated.ts` must be refreshed to the current backend contract and pass `npm run api:check` before Phase 2B can be marked validated.

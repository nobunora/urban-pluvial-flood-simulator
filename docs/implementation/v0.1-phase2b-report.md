# v0.1 Phase 2B implementation report

## Disposition

`review-pending`

Base implementation commit: `0e3308e` (`Implement Phase 2B geocoder and JMA rainfall catalogs`).
Review-fix implementation commit: `a332b54ac2c00a3cb03ca7c0a72ba6d1ba81b318`.
The Phase 2B review fixes pass locally and await review on Draft PR #12.

Phase 0, Phase 1, and Phase 2A behavior remains covered by the regression suite.
Phase 3+ functionality was not implemented.

## Implemented boundary

- `floodsim/providers/geocoder.py` uses only the approved CSIS Simple Geocoding
  endpoint, the shared network/retry policy, defensive XML parsing, WGS84
  validation, candidate ordering, and a ten-candidate cap.
- `floodsim/api/routes_geocode.py` exposes `GET /api/v1/geocode` with trimmed
  and bounded input, CSIS attribution metadata, and stable 400/503 errors.
- `floodsim/providers/jma.py` parses official AMeDAS station CSV and explicit
  official rainfall ranking labels for 10-minute, 1-hour, and daily events.
  Runtime loading validates and caches the packaged JSON catalog; it does not
  scrape JMA at application startup or API request time.
- `data/jma/stations.json` contains 1,286 precipitation-capable stations.
  `data/jma/rainfall_extremes.json` contains six deterministic events for two
  stations from the packaged official ranking snapshots, with
  `profile_available=false`. This is limited historical-event coverage, not
  nationwide extreme-event coverage. Historical station search excludes the
  other 1,284 stations because they have no packaged event choices.
- `floodsim/api/routes_rainfall.py` exposes nearest stations, station extremes,
  and event lookup with stable 404/503 errors.
- `scripts/build_jma_rainfall_catalog.py` provides deterministic maintenance
  generation from official source snapshots and supports focused station
  limits. `data/jma/sources/README.md` records the exact rebuild command. The
  controlled timestamp, packaged AMeDAS archive, two packaged ranking pages,
  station/duration mapping, and rank-two cutoff reproduce both committed JSON
  catalogs; a focused test compares the generated payloads to them exactly.
- `web/openapi.json` and `web/src/api/generated.ts` are generated by
  `openapi-typescript`; `npm run api:check` detects drift.

## Verification

Focused and regression checks:

- `.venv\Scripts\python.exe -m pytest -q tests/test_phase2b.py tests/test_phase1_api.py tests/test_phase1_openapi.py` — **PASS**, 30 passed, 2 warnings.
- `.venv\Scripts\python.exe -m pytest -q` — **PASS**, 51 passed, 1 skipped, 2 warnings.
- `.venv\Scripts\python.exe -m ruff check floodsim tests scripts/build_jma_rainfall_catalog.py` — **PASS**.
- `.venv\Scripts\python.exe -m mypy floodsim` — **PASS**, 22 source files.
- Ruff `0.16.5`; mypy `2.3.1`.
- Import Linter — **NOT RUN**, no repository configuration and module not installed.
- ty — **NOT RUN**, module not installed.
- deptry — **NOT RUN**, module not installed.

Frontend and generated contract checks:

- `cd web; npm ci` — **PASS**, 0 vulnerabilities. npm reported one existing
  transitive deprecation warning for `whatwg-encoding`.
- `npm run api:generate` — **PASS**.
- `npm run api:check` — **PASS**, generated OpenAPI and TypeScript match.
- `npm run typecheck` — **PASS**.
- `npm run lint` — **PASS**.
- `npx --no-install oxlint src scripts` — **PASS**, Oxlint `1.81.0`.
- `npm test -- --run` — **PASS**, 1 test.
- `npm run build` — **PASS**.

Live contract smokes, separate from deterministic tests:

- CSIS generic place query: **PASS**, HTTP/XML parsed to one valid WGS84
  candidate at `139.753632,35.694004`. The raw query and provider payload were
  not logged by the application.
- JMA AMeDAS source: **PASS**, HTTP 200, 53,494 bytes, 1,286 precipitation
  stations parsed from the official station archive and fixed as the packaged
  reproducibility snapshot.
- JMA historical ranking pages: **PASS**, both official pages returned HTTP
  200; each parsed 33 events with explicit durations `[10, 60, 1440]` minutes
  before the documented deterministic selection.

## Known risks

- CSIS and JMA are external sources and may change or become unavailable.
- Packaged rainfall events intentionally expose no observed temporal profile;
  runtime profile scraping remains out of scope.

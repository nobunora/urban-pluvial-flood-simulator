# v0.1 Phase 0 Compatibility Report

## Disposition

`spec-change-required`

This disposition follows the Phase 0 stop rule: the required SFINCS execution
check could not be completed. This is an environment/access blocker, not a
confirmed HydroMT-SFINCS model-format incompatibility. Re-run the same harness
after supplying a permitted SFINCS executable.

## Baseline

- Python: `3.12.10` (`MSC v.1943 64 bit (AMD64)`).
- Operating system: Microsoft Windows 10 Pro, build `19045`, x64.
- HydroMT-SFINCS: `2.0.0rc3`, official tag commit `82e58ee85136cf5155c92b42bb9a397869ed8035`.
- HydroMT: `1.4.1`.
- SFINCS target: `v2.4.0_Galibier_release`, official source commit `1f1d8286520a8709b2e513854dff18a3616583db`.
- Other exact packages: `geopandas 1.1.4`, `netCDF4 1.7.4`, `numpy 2.5.2`, `pyproj 3.7.2`, `rasterio 1.5.1`, `scipy 1.18.1`, `shapely 2.1.2`, `xarray 2026.7.0`, `xugrid 0.15.3`, `pytest 9.1.1`.

## Environment and Install Commands

The repository now provides `environment.yml`. The tested isolated Windows
environment used the following equivalent commands; `<phase0-root>` was a
temporary directory outside the repository:

```powershell
python -m pip install --target <phase0-root>\site uv
<phase0-root>\site\bin\uv.exe python install 3.12.10 --install-dir <phase0-root>\python
<phase0-root>\python\cpython-3.12.10-windows-x86_64-none\python.exe -m venv <phase0-root>\venv
<phase0-root>\site\bin\uv.exe pip install --python <phase0-root>\venv\Scripts\python.exe <hydromt_sfincs-v2.0.0-rc3-checkout> netCDF4 pytest
```

The checkout was obtained from the official `Deltares/hydromt_sfincs`
`v2.0.0-rc3` tag. The harness clears a non-numeric inherited `DEBUG` value
only while importing this pre-release package; the observed shell value was
`DEBUG=release` and otherwise causes a Pydantic import error.

## Checks

| Check | Result | Evidence |
| --- | --- | --- |
| Regular model build | PASS | Harness writes `sfincs.inp`, `sfincs.dep`, `sfincs.msk`, `sfincs.ind`, and `sfincs.manning`. |
| Quadtree model build | PASS | Harness writes `sfincs.inp` and `sfincs.nc`; 472 cells at levels 1/2/3 = 28/44/400. |
| Subgrid model build | PASS | Harness writes `sfincs_subgrid.nc`; source raster is 5 m and hydraulic grid is 10 m. |
| SFINCS execution | BLOCKED | No permitted `sfincs.exe` was supplied or found locally. |
| Result read | PASS* | Direct Xarray reader validates the official example NetCDF contract; an execution-produced result remains pending. |

`*` The result-read proof checks `h`, `hmax`, `zs`, `zb`, and `msk`, with
dimensions `time,n,m`, `timemax,n,m`, `time,n,m`, `n,m`, and `n,m`. Depth units
are metres; `msk` uses `-`; non-finite depth values are rejected and `msk`
identifies active cells.

## Exact Validation Commands and Results

```text
git diff --check                                      PASS
python -m py_compile scripts/sfincs_phase0_spike.py  PASS
.venv\Scripts\python.exe -m pytest -q              PASS (4 passed, 1 skipped)
<Python 3.12 venv> -m pytest -q                      PASS (6 passed, 185 warnings)
<Python 3.12 venv> scripts/sfincs_phase0_spike.py --out-dir <temporary-output>
                                                     PASS for all three builds; execution BLOCKED
```

The focused Python 3.12 test run took 32.10 seconds. Warnings came from
upstream HydroMT/affine/scipy/NumPy pending deprecations. `ruff` and `mypy`
are not installed; repository-local `scripts/repo_query.py` and
`scripts/analyze.py` are absent and were recorded as optional-wrapper-
unavailable. CodebaseMemory was ready before the change; its post-change
refresh attempt failed because its transport closed.

## Output and File Contracts

- Regular input: `sfincs.inp`, `sfincs.dep`, `sfincs.msk`, `sfincs.ind`,
  `sfincs.manning`.
- Quadtree input: `sfincs.inp`, `sfincs.nc`.
- Subgrid input: `sfincs.inp`, `sfincs.msk`, `sfincs.ind`,
  `sfincs_subgrid.nc`.
- NetCDF result expected from `outputformat = net`: `sfincs_map.nc`; optional
  observation output is `sfincs_his.nc`.
- Required map variables for the read proof: `h`, `hmax`, `zs`, `zb`, `msk`.

## SFINCS Acquisition and License

The SFINCS source was cloned from the official GitHub release tag. The
official pre-compiled Windows download is the Deltares software portal, which
returned a login-required message for the unauthenticated validation request.
No binary was committed or redistributed. The source and HydroMT-SFINCS
repositories declare GPL-3.0 licensing. The executable license reference and
SHA-256 cannot be recorded until a permitted executable is supplied.

## Warnings and Unresolved Questions

- The pre-release plugin emits `No region component found in components.` for
  the minimal model instances; construction still succeeds.
- The plugin currently fails to import if an inherited non-numeric `DEBUG`
  value is present; the harness documents and isolates this workaround.
- Provide an authenticated/permitted SFINCS v2.4.0 executable, its license
  notice, and its acquisition route before Phase 1.
- Confirm whether the final engine bootstrap may use the Deltares portal or
  must use a separately approved local developer override.

## Final PR Summary

```text
Disposition: spec-change-required
SFINCS: v2.4.0 Galibier (target; executable not supplied)
HydroMT-SFINCS: 2.0.0rc3
Python: 3.12.10
Regular build: PASS
Quadtree build: PASS
Subgrid build: PASS
SFINCS execution: BLOCKED
Result read: PASS* (fixture contract; engine-produced result pending)
Existing tests: .venv\Scripts\python.exe -m pytest -q -> 4 passed, 1 skipped
New focused tests: Python 3.12 venv -m pytest -q -> 6 passed, 185 warnings
Distribution/license blocker for later phases: yes - executable access/license manifest unresolved
Unresolved questions: permitted SFINCS executable and managed acquisition policy
```

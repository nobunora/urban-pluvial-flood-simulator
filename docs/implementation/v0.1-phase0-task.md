# v0.1 Phase 0 Compatibility Spike

## Specification

- Path: `docs/specs/v0.1-implementation-spec.md`
- Instruction: `.codex/v0.1-phase0.md`
- Revision / commit: `d2ac59515463b3f534ee67cfac49a95d539b1c84`

## Repository Review Status

`blocked`

The repository had no HydroMT-SFINCS/SFINCS integration, Phase 0 harness, or
reproducible environment file. Existing preprocessing scripts and tests are
independent of the spike. The pinned HydroMT-SFINCS source is available from
the official GitHub tag, but the required SFINCS executable could not be
obtained without an authenticated official download.

## Affected Paths

- `scripts/sfincs_phase0_spike.py` - deterministic regular, quadtree, subgrid,
  and direct NetCDF result-read proof.
- `tests/test_sfincs_phase0.py` - focused build and result-read tests.
- `environment.yml` - Python 3.12 and pinned Phase 0 dependency environment.
- `docs/implementation/v0.1-phase0-report.md` - evidence and disposition.

## Existing Contracts / Invariants

- Existing `scripts/` preprocessing entrypoints remain unchanged.
- Existing native `src/solver.cpp` remains unchanged.
- No generated model outputs are tracked.
- SFINCS is not bundled or redistributed by this change.

## Verification Plan

```text
git diff --check
python -m py_compile scripts/sfincs_phase0_spike.py
.venv\Scripts\python.exe -m pytest -q
<Python 3.12 Phase 0 environment> -m pytest -q
<Python 3.12 Phase 0 environment> scripts/sfincs_phase0_spike.py --out-dir <temporary-output> --sfincs-exe <permitted-sfincs.exe>
```

## Implementation Result

Added the smallest harness needed to construct and inspect the three model
forms. The SFINCS process runner accepts an explicit executable, records its
SHA-256, and reads `sfincs_map.nc` directly with Xarray.

## Verification Evidence

See `docs/implementation/v0.1-phase0-report.md` for exact commands and
results. Regular, quadtree, subgrid, and direct result-read tests pass in the
Python 3.12.10 temporary environment. SFINCS execution remains blocked.

## Risks / Open Questions

- Obtain a permitted SFINCS v2.4.0 Galibier Windows executable and its license
  reference, then rerun the engine and result-read checks.
- Confirm the final managed-engine acquisition and redistribution policy before
  later packaging phases.

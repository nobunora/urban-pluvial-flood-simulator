# v0.1 Phase 0B User Handoff

This note records the only user-side prerequisite for the next validation pass.

## User action

Obtain and install a legitimate SFINCS **v2.4.0 Galibier** executable locally.

Before invoking the next Codex task, expose its absolute path:

```powershell
$env:SFINCS_EXE = 'C:\path\to\sfincs.exe'
```

Do not commit the executable or the machine-specific absolute path.

## Next Codex instruction

Use exactly:

```text
.codex/v0.1-phase0b.md
```

The next pass must only close the SFINCS execution and execution-produced result-read checks. Phase 1 is not permitted until the Phase 0 report disposition becomes `validated`.
